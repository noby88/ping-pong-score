'use client';

import { ArrowLeft } from 'lucide-react';
import { FloatingButton } from './styles';

const BackButton = () => (
  <FloatingButton>
    <ArrowLeft />
  </FloatingButton>
);

export default BackButton;
